Refer to canonical agent instructions in `.claude/CLAUDE.md`.

This checkout is a fork (`origin` = `FrontEndSharedProject/brave-core-fingerprint`,
`upstream` = `brave/brave-core`); custom work lives on the `fingerprint` branch,
`master` stays a pure upstream mirror. See the "Fork & Upstream Sync" section in
`.claude/CLAUDE.md` for the sync commands.
